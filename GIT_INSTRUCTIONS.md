# 📝 Инструкция по сохранению изменений в GitHub

## 🚀 Быстрая команда (3 шага)

```bash
# 1. Добавить все изменения
git add .

# 2. Создать коммит с описанием
git commit -m "Описание изменений"

# 3. Отправить на GitHub
git push
```

## 📋 Подробная инструкция

### Шаг 1: Проверить, что изменилось
```bash
git status
```
Показывает:
- ✅ **Изменённые файлы** (modified)
- ➕ **Новые файлы** (untracked)
- 🗑️ **Удалённые файлы** (deleted)

### Шаг 2: Добавить файлы в staging
```bash
# Добавить конкретный файл
git add events/views.py

# Добавить все изменения
git add .

# Добавить только изменённые файлы (без новых)
git add -u
```

### Шаг 3: Создать коммит
```bash
git commit -m "Краткое описание изменений"
```

**Примеры хороших сообщений коммита:**
- `"Добавлена страница профиля пользователя"`
- `"Исправлена ошибка в форме регистрации"`
- `"Обновлён дизайн списка событий"`
- `"Добавлена функция поиска волонтёров"`

### Шаг 4: Отправить на GitHub
```bash
git push
```

Если первая загрузка ветки:
```bash
git push -u origin main
```

## 💡 Полезные команды

### Просмотр изменений перед коммитом
```bash
# Показать изменения в файлах
git diff

# Показать изменения в конкретном файле
git diff events/views.py
```

### Отмена изменений
```bash
# Отменить изменения в файле (до git add)
git checkout -- имя_файла.py

# Убрать файл из staging (после git add)
git reset имя_файла.py

# Отменить последний коммит (оставить изменения)
git reset --soft HEAD~1
```

### Просмотр истории коммитов
```bash
# Последние коммиты
git log

# Короткий вид
git log --oneline

# Последние 5 коммитов
git log -5
```

### Получение изменений с GitHub
```bash
# Скачать изменения с GitHub
git pull

# Или отдельно fetch + merge
git fetch
git merge origin/main
```

## ⚠️ Важно!

1. **Всегда проверяйте** `git status` перед коммитом
2. **Пишите понятные** сообщения коммитов
3. **Не коммитьте**:
   - `venv/` (виртуальное окружение)
   - `db.sqlite3` (база данных)
   - `__pycache__/` (кеш Python)
   - `media/` (загруженные файлы)

Эти файлы уже в `.gitignore` и будут автоматически игнорироваться.

## 🔄 Типичный рабочий процесс

```bash
# 1. Внесли изменения в код
# ... редактируете файлы ...

# 2. Проверили, что изменилось
git status

# 3. Добавили изменения
git add .

# 4. Создали коммит
git commit -m "Описание изменений"

# 5. Отправили на GitHub
git push
```

---

**Готово!** Теперь ваши изменения сохранены в GitHub 🎉

